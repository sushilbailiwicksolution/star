/**
 * This contains event severity enum
 * @enum {low}
 * @enum {high}
 *  @enum {medium}
 */

export enum EventSeverityEnum {
    low = "low", 
    high = "high", 
    medium = "medium"
}