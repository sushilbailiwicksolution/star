/**
 * This is status enum
 * @enum {ACTIVE} status 
 * @enum {DELETED} status
 * @enum {DISABLED} status
 * @enum {PENDING} status
 */

export enum StatusEnum {
    ACTIVE = "ACTIVE", 
    DELETED = "DELETED", 
    DISABLED = "DISABLED", 
    PENDING = "PENDING"
}